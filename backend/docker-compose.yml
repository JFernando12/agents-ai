version: '3.8'

services:
  backend-ai:
    build: .
    container_name: backend-ai
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    restart: unless-stopped
